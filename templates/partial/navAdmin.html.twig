<nav
    class="navbar navbar-expand-lg navbar-light bg-light border-bottom shadow-sm"
>
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand fw-bold" href="{{ path('app_home_admin') }}">
            FitProgress Admin
        </a>

        <!-- Burger menu mobile -->
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarAdmin"
            aria-controls="navbarAdmin"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Links -->
        <div class="collapse navbar-collapse" id="navbarAdmin">
            <ul class="navbar-nav me-auto">
                <!-- Liens communs -->
                <li class="nav-item">
                    <a
                        class="nav-link text-dark"
                        href="{{ path('app_home_admin') }}"
                        >Dashboard</a
                    >
                </li>
                <li class="nav-item">
                    <a
                        class="nav-link text-dark"
                        href="{{ path('app_dashboard') }}"
                        >Vue Utilisateur</a
                    >
                </li>

                <!-- Gestion admin -->
                <li class="nav-item">
                    <a
                        class="nav-link text-dark"
                        href="{{ path('admin_goal') }}"
                        >Gérer les Objectifs</a
                    >
                </li>
                <li class="nav-item">
                    <a
                        class="nav-link text-dark"
                        href="{{ path('admin_user_index') }}"
                        >Mes Utilisateurs</a
                    >
                </li>
            </ul>

            <!-- Infos user -->
            <ul class="navbar-nav">
                {% if app.user %}
                <li class="nav-item">
                    <span class="nav-link text-dark">
                        👤 {{ app.user.name ?? app.user.email }}
                    </span>
                </li>
                <li class="nav-item">
                    <a
                        class="nav-link text-dark"
                        href="{{ path('app_logout') }}"
                        >Déconnexion</a
                    >
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
