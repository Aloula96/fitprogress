{% extends 'base.html.twig' %} {% block title %}Choix de l'objectif{% endblock
%} {% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('styles/goal.css') }}" />
{% endblock %} {% block javascripts %}
{{ parent() }}
<script src="{{ asset('js/imc.js') }}"></script>
{% endblock %} {% block body %}
<div
    class="goal-container"
    data-height="{{ user.height|default(0) }}"
    data-weight="{{ user.weight|default(0) }}"
>
    <div class="back-button">
        <a href="{{ path('app_home') }}">
            <span class="back-icon">←</span>
            <span>Retourner au site</span>
        </a>
    </div>

    <h2 class="goal-title">Quel est ton objectif?</h2>

    {% for label, messages in app.flashes %} {% for message in messages %}
    <div class="alert alert-{{ label }}">{{ message }}</div>
    {% endfor %} {% endfor %}

    <form method="post" class="goal-form">
        <div class="goal-options">
            <label class="goal-card">
                <input
                    type="radio"
                    name="goal"
                    value="prise_de_masse"
                    required
                />
                <div class="goal-content">
                    <div class="goal-icon">🏋️</div>
                    <h3>Prendre du muscle</h3>
                    <p>Programme basé sur la musculation progressive</p>
                </div>
            </label>

            <label class="goal-card">
                <input
                    type="radio"
                    name="goal"
                    value="perte_de_poids"
                    required
                />
                <div class="goal-content">
                    <div class="goal-icon">⚡</div>
                    <h3>Perdre du poids</h3>
                    <p>Programme axé sur la perte de poids</p>
                </div>
            </label>

            <label class="goal-card">
                <input type="radio" name="goal" value="maintien" required />
                <div class="goal-content">
                    <div class="goal-icon">⚖️</div>
                    <h3>Maintenir du poids</h3>
                    <p>Programme axé sur le maintien</p>
                </div>
            </label>
        </div>

        <div class="weight-input">
            <label for="target_weight">Poids cible</label>
            <input
                type="number"
                id="target_weight"
                name="target_weight"
                value="70"
                min="40"
                max="200"
                step="0.1"
            />
        </div>

        <div class="imc-display">
            <p>
                <strong>IMC actuel :</strong> <span id="current-imc">N/A</span>
            </p>
            <p>
                <strong>Catégorie :</strong> <span id="imc-category">N/A</span>
            </p>
        </div>

        <button type="submit" class="submit-btn">Valider mon objectif</button>
    </form>
</div>
{% endblock %}
